import 'package:flutter/material.dart';

class HomeFail extends StatelessWidget {
  const HomeFail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('fail home fetching');
  }
}
